<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        /* Additional styles if needed, though Tailwind should cover most */
        body {
            font-family: sans-serif;
        }
    </style>
</head>
<body class="bg-white"> -->
    <%- include("templates/header") %>

    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    
    <div class="max-w-sm mx-auto min-h-screen flex flex-col">

        <!-- Header -->
        <header class="py-4 px-4 flex items-center justify-between border-b border-gray-300 sticky top-0 bg-white z-10">
            <button class="text-2xl text-gray-700">
                <!-- Placeholder for back arrow icon -->
                <i class="fas fa-chevron-left"></i>
            </button>
            <h1 class="text-xl font-semibold text-gray-800">Shopping Cart</h1>
            <button class="text-2xl text-gray-700">
                <!-- Placeholder for shopping cart icon -->
                <i class="fas fa-shopping-cart"></i>
            </button>
        </header>

        <!-- Cart Items -->
        <main class="flex-grow p-4 space-y-4">
            <!-- Single Cart Item -->
            <div class="bg-gray-200 rounded-lg p-3 flex items-center shadow">
                <!-- Product Image Placeholder -->
                <div class="w-20 h-20 bg-gray-300 rounded mr-3 flex-shrink-0">
                    <!-- You can put an <img> tag here later -->
                    <!-- <img src="path/to/egg-image.png" alt="Egg" class="w-full h-full object-cover rounded"> -->
                </div>

                <!-- Product Details -->
                <div class="flex-grow">
                    <h2 class="text-md font-semibold text-gray-800">Egg</h2>
                    <p class="text-sm font-bold text-gray-700">$6.99</p>
                    <div class="flex items-center mt-1">
                        <span class="text-xs text-gray-600 mr-2">Qty</span>
                        <div class="flex items-center bg-white rounded-md border border-gray-300">
                            <button class="px-2 py-1 text-sm text-gray-700">
                                <!-- Placeholder for minus icon -->
                                <i class="fas fa-minus"></i>
                            </button>
                            <span class="px-3 py-1 text-sm font-medium text-gray-800">1</span>
                            <button class="px-2 py-1 text-sm text-gray-700">
                                <!-- Placeholder for plus icon -->
                                <i class="fas fa-plus"></i>
                            </button>
                        </div>
                    </div>
                </div>

                <!-- Delete Button -->
                <button class="ml-4 text-xl text-gray-600 hover:text-red-500 flex-shrink-0">
                    <!-- Placeholder for delete icon -->
                    <i class="fas fa-trash-alt"></i>
                </button>
            </div>
            <!-- Add more cart items here if needed -->

        </main>

        <!-- Footer / Checkout Button -->
        <footer class="p-4 sticky bottom-0 bg-white border-t border-gray-200">
            <button class="w-full bg-gray-800 text-white py-3 px-4 rounded-lg flex justify-between items-center text-lg font-semibold hover:bg-gray-700">
                <span>$6.99</span>
                <div class="flex items-center">
                    <span>Checkout</span>
                    <!-- Placeholder for right arrow icon -->
                    <i class="fas fa-arrow-right ml-2"></i>
                </div>
            </button>
        </footer>

    </div>

<!-- START TEST CODE FOR PAYMENT FEATURE-->
<button id="checkout">Click here to test checkout</button>

<script>
    document.getElementById("checkout")
        .addEventListener('click', async () => {
            const payload = {
                sellerId: "6827313b146f2df7484f2735", // clinton's test seller account
                cartItems: [
                    { produce: "Apples",  price: 3.00, quantity: 4 },
                    { produce: "Carrots", price: 1.50, quantity: 2 },
                    { produce: "Kale", price: 2.25, quantity: 1 }
                ]
            };

            try {
                const res = await fetch('/checkout', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(payload),
                });

                const data = await res.json();
                console.log('Stripe redirect URL:', data.url);

                window.open(data.url);
            } catch (err) {
                console.error('Checkout error:', err);
            }
        });
</script>
<!-- END TEST CODE FOR PAYMENT FEATURE -->

<!-- </body>
</html> -->
<%- include("templates/footer") %>

